{
  "name": "storefront_backend",
  "version": "1.0.0",
  "description": "node project",
  "main": "server.js",
  "scripts": {
    "start": "node ./src/server.js",
    "dev": "nodemon ./src/server.js",
    "prettier": "prettier --write src/**/*.js",
    "clean": "rm -rf dist/ || true",
    "build1": "npm run clean && tsc && cp .npmrc dist/.npmrc && cp package.json dist/package.json && cd dist && zip -r archive.zip .&& cd ..",
    "build": "tsc && cp .npmrc dist/.npmrc && cp package.json dist/package.json && cd dist && zip -r Archive.zip . && cd ..",
    "jasmine": "set ENV=test jasmine",
    "migrate": "db-migrate --env dev up && db-migrate up",
    "test-migrate": "set ENV=test && npx tsc && db-migrate --env test up && npx tsc && jasmine",
    "test-h": "tsc && set ENV=test && db-migrate --env test up && jasmine && db-migrate --env test reset",
    "reset-test": "set ENV=test && npx db-migrate reset --env test",
    "tsc": "tsc",
    "lint": "eslint . --ext .js",
    "lint_fix": "eslint . --ext .js --fix"
  },
  "author": "tareq sheta",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.25.0",
    "jasmine": "^4.4.0",
    "jasmine-spec-reporter": "^7.0.0",
    "nodemon": "^3.1.10",
    "prettier": "^2.7.1",
    "supertest": "^6.3.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.1"
  },
  "type": "module",
  "keywords": []
}
